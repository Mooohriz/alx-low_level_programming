#include <stdio.h>
/**
*main - ptints the for the program
*@argc: is a counter for argument for int
*@argv: is a counter fir value of the argument to the char
*Return: returns always success
*/

int main(int argc, char *argv[])
{
(void)argv;
printf("%d\n", (argc - 1));

return (0);
}

